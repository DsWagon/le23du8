<%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
  <%= f.search_field :title_or_content_cont_any,
  placeholder: "Search...",
  class: "form-control me-2 d-flex" %>
  <%= f.submit "Search", class:"btn btn-flat mx-2" %>
<% end %>
