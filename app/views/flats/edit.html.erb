


<div class="container form-container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
    <h2 style="text-align:center;">Modifier les informations sur mon logement</h2>
      <%= simple_form_for @user_flat, url: flat_path(@user_flat), method: :patch do |f| %>
        <div class="form-inputs">
          <%= f.input :floor, label: "Étage",
          input: "select",
          required: true,
          collection: 0..5, selected: 2 %>
          <%= f.input :surface, label: "Surgace",
          required: true,
          integer: true %>
          <%= f.input :lot, label: "Lot",
          required: true,
          integer: true %>
        </div>
        <div class="form-actions">
        <%= button_tag type: 'submit', class: "btn btn-rounded" do %>Valider mes modifications<% end %>
      </div>
      <% end %>
      <hr>
      <%= link_to 'Back', "/profile", class: 'btn btn-flat' %>
    </div>
  </div>
</div>
