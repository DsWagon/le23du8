<div class="container">
  <h1>Show de la quote</h1>
  <div class="row">
    <div class="col-md-12 mb-12">
      <div class="card">
        <div class="card-header"><%= @quote.meeting.title%>
        </div>
        <div class="card-body">
          <p class="card-text"><%= @quote.company_name %></p>
          <p class="card-text"><%= @quote.description %></p>
          <p><%= @quote.price %></p>
          <p><%= @quote.date_start %></p>
          <p><%= @quote.date_end %></p>
          <p><%= @quote.meeting.title%></p>
          <hr>
          <%= link_to 'Voir les devis', quotes_path, class: 'btn btn-flat' %>
          <%= link_to edit_quote_path(@quote) do %>
              <i class="fa-solid fa-edit fa-lg circle-icon-edit mx-2" style="color: #fd7e14;"></i>
            <% end %>
          <%= link_to quote_path(@quote), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } do %>
              <i class="fa-solid fa-trash fa-lg circle-icon-delete mx-2" style="color: #e7081f;"></i>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
