<div class="container">

  <button class="btn btn-back" onclick="window.history.back()">
    <i class="fa-solid fa-arrow-left fa-xs"></i>
    Retour
  </button>

  <div class="row">
    <div class="col-md-12 mb-12">
      <div class="card">
        <div class="card-header"><%= @quote.meeting.title%>
        </div>
        <div class="card-body">
          <p class="card-text"><%= @quote.company_name %></p>
          <p class="card-text"><%= @quote.description %></p>
          <p><%= @quote.price %></p>
          <p><%= @quote.date_start %></p>
          <p><%= @quote.date_end %></p>
          <p><%= @quote.meeting.title%></p>
          <hr>
          <%= link_to meeting_path(@quote.meeting), class: 'btn btn-back' do %>
            <i class="fa-solid fa-arrow-right fa-sm"></i>
            Voir la réunion
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
