<div class="container devise-container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <h2 style="text-align:center;">Administration</h2>
      <%= link_to "Nouvelle Réunion", "/meetings/new", class: "btn btn-flat btn-login" if current_user.admin? %>
      <%= link_to "Réunions", "/meetings", class: "btn btn-flat btn-login" if current_user.admin? %>
      <%= link_to "Nouveau Devis", "/quotes/new", class: "btn btn-flat btn-login" if current_user.admin? %>
      <%= link_to "Devis", "/quotes", class: "btn btn-flat btn-login" if current_user.admin? %>
    </div>
  </div>
</div>
