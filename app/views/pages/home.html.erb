<div class="container-home d-flex justify-content-center align-items-center flex-wrap p-2">
  <div class="row mt-5">
  <div class="col-md-12">
    <div class="row">
      <div class="col-md-12 ">
        <h1 class="text-center">Bienvenue sur <strong class="highlight">Le23du8</strong> !</h1>
        <p class="text-center">Notre site a été spécialement conçu pour vous offrir un accès facile à toutes les informations concernant notre copropriété, ainsi qu'à une multitude de ressources utiles.</p>
        <% if user_signed_in? %>
          <p class="text-center">Votre rôle : <%= current_user.role %></p>
        <% end %>
      </div>
    </div>
    <h1>Derniers Posts</h1>
<ul>
  <% @posts.each do |post| %>
    <li>
      <h2><%= post.title %></h2>
      <p><%= post.content.truncate(120) %></p>
      <!-- Ajoutez d'autres détails du post que vous souhaitez afficher -->
    </li>
  <% end %>
</ul>
  </div>
</div>
